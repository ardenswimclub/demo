---
const { id, title, bg } = Astro.props;
---

<section id={id} class={`py-20 px-8 ${bg}`}>
  <div class="max-w-4xl mx-auto prose-xl prose-slate">
    <h2 class="text-3xl font-semibold mb-4">{title}</h2>
    <slot />
  </div>
</section>
