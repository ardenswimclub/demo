---
import Container from "@/components/container.astro";
import BaseLayout from "./BaseLayout.astro";

export interface Props {
  title: string;
  noContainer?: boolean;
  menu: string;
}

const { title, noContainer, menu } = Astro.props;

---


<BaseLayout {menu} {title} {noContainer}>
    <slot />
</BaseLayout>