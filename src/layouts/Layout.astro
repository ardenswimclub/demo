---
import Container from "@/components/container.astro";
import BaseLayout from "./BaseLayout.astro";

export interface Props {
  title: string;
  noContainer?: boolean;
  menuKey: string;
}

const { title, noContainer, menuKey } = Astro.props;

---


<BaseLayout {menuKey} {title} {noContainer}>
    <slot />
</BaseLayout>